<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.care.mybatis.Membermapper">
	<select id="listAll" resultType="dto">
		select * from member
	</select>
	<select id="list" resultType="dto" parameterType="String">
		select * from member where id=#{id}
	</select>
	<!--  <insert id="savedata" parameterType="dto">
		insert into member values(#{id},#{pw},#{name},#{email},#{tel},#{tel1},#{tel2},#{name},)
	</insert>-->
	
</mapper>
















